---
name: user-working-style
description: How the primary developer works — build/verify habits, git preferences, collaboration style, and what they expect from agents. (Replace this example with your real conventions.)
metadata:
  type: user
---

This is an example memory file showing the format. Replace it with a real one (or delete it).

A memory holds **one** fact or learning. Keep it specific and behavioral. Examples of what belongs here:
- How the developer builds and tests, and what they do themselves vs. delegate.
- Git preferences (e.g. "keeps commits local and pushes manually — never push without being asked").
- Recurring gotchas that already cost time once.
- Cross-references to related memories using `[[wikilink]]` style.

What does NOT belong here: anything the repo already encodes (code structure, git history, AGENTS.md content, the skills).
